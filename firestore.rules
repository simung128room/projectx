rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Global Deny - Secure BFF Architecture
    // All traffic must go through the Node.js API Gateway (Admin SDK)
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
